alias(NGLfserv::init)
{
	if ($objects.exists(%NGLfserver)){
		debug call destrutor nglfserver
	 	delete -I %NGLfserver
	}
	# register classes
	NGLfserv::classes::register
	
	# init events
	NGLfserv::events::init	
	
	 # load config
	NGLfserv::config::load
}

